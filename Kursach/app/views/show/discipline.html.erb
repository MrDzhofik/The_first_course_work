<%= stylesheet_link_tag "table.css"%>

<%= button_to 'Назад', root_path, class: "back_to_all btn btn-outline-primary", method: :get %>
<div width=400px>
<h1 align="center">Дисциплины</h1>
    <table class="table table-bordered table-sm table-hover">
        <th scope="col">Идентификатор</th>
        <th scope="col">Название</th>
        <th scope="col">Кафедра</th>
        <% @discipline.each do |teacher| %>
        <tr scope="row">
            <td><%= teacher['discipline_id']%></td>
            <td><%= teacher['discipline_name']%></td>
            <td><%= teacher['department']%></td>
        </tr>
        <% end %>
    </table>
</div>
